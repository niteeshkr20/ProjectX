//let initialState = [];


let initialState = [];
    
    

        
const contactlist = (state = initialState, action) => {
    switch (action.type) {
             
        default:            
            return state      
              
    }
}

export default contactlist